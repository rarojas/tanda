import { StyleSheet } from “react-native”;
let iconSize = 24;
let primaryColor = '#1EC267'
export default StyleSheet.create({
   button: {
     borderRadius: 5,
     backgroundColor : primaryColor
   }
});
